server:
  port: 8001
  servlet:
    context-path: /wms

spring:
  application:
    name: wms-center
  profiles:
    active: dev

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.liu.code.wmscenter.entity
  configuration:
    map-underscore-to-camel-case: true
